Entity Framework Core (Core means it works with multiple OS)
If .NET core version is 3.1 (Then EF Core version should be of the range 3.__(3.0/3.1/3.2) something only [For being compatible, then only code is autogenerated])
EF Core is an Object relational mapping (ORM) - It automatically generates crude operations required for working with database.

EF Migrations
The migrations feature in EF Core provides a way to incrementally update the database schema to keep it in sync with the application's data model while preserving existing data in the database.

